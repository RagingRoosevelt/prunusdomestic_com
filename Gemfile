source "https://rubygems.org"

# Matches what GitHub Pages runs, so a local build can't diverge from the
# deployed one. `github-pages` pins jekyll and the whitelisted plugins.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
